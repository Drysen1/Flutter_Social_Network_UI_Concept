class ProfileModel {
  int id;
  String firstName;
  String lastName;
  String information;
  String location;
  String image;

  ProfileModel(this.id, this.firstName, this.lastName, this.information, this.location, this.image);
}